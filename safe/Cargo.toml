[package]
name = "safe-liblzma"
version = "0.1.0"
edition = "2021"
publish = false
build = "build.rs"

[lib]
name = "lzma"
crate-type = ["cdylib", "staticlib", "rlib"]

[dependencies]
libc = "0.2"

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"
